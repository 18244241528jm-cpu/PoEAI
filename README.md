# PoEAI

PoEAI 相关学习资料与讲义。

## 内容概览

- **单视图位姿估计**：几何与线性求解、平面单应、April Tag、PnP 等
- **Procrustes 与 RANSAC**：Procrustes、Kabsch、外点、RANSAC 思想与实现

## 本地 Git 状态

当前已完成：

- 初始化 Git 仓库
- 添加 `.gitignore`
- 配置远程：`https://github.com/18244241528jm-cpu/PoEAI.git`
- 已提交所有文件（Initial commit）

## 推送到 GitHub

若远程仓库 `PoEAI` 尚未创建，请先在 [GitHub 新建仓库](https://github.com/new?name=PoEAI)（不要勾选 README、.gitignore 等）。

推送命令（将 `YOUR_TOKEN` 替换为你的 Personal Access Token）：

```bash
cd "c:\Users\30518\OneDrive\Desktop\PoEAI"
git push https://18244241528jm-cpu:YOUR_TOKEN@github.com/18244241528jm-cpu/PoEAI.git main
```

或使用 Git 凭据管理器，直接执行：

```bash
git push -u origin main
```
